name: Copilot coding agent

on:
  dynamic

jobs:
  copilot:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
      
      - name: Show git state (diagnostics)
        run: |
          echo "GITHUB_REF=$GITHUB_REF"
          git --version
          git remote -v
          git fetch --all --tags --prune
          git branch -a
          git rev-parse --show-toplevel || true
          git status --porcelain || true
      
      - name: Ensure origin/main is available
        run: |
          echo "Fetching origin/main..."
          git fetch origin main:refs/remotes/origin/main --depth=1 || echo "Warning: Could not fetch origin/main"
          echo "Verifying refs..."
          if git show-ref --verify --quiet refs/remotes/origin/main; then
            echo "✓ origin/main is available"
          elif git rev-parse --verify --quiet main; then
            echo "✓ main is available locally"
          else
            echo "Warning: Neither origin/main nor local main found"
          fi
      
      - name: Configure git for automated commits
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
